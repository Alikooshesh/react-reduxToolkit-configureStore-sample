import { useContext } from "react"
// import {PlayersContext} from '../../App'

const Input = ({onChangeHandler , value})=>{


    // const playersState = useContext(PlayersContext)

    return(
        <>
        {/* {console.log(playersState.players)}
        <input value={playersState.players.playerOne.name} onChange={(e)=> playersState.setPlayers({...playersState.players , playerOne : {...playersState.players.playerOne , name : e.target.value}})}/> */}
        </>
    )
}

export default Input